# DOCSTYLE — 文档规范

[`METHOD.md`](METHOD.md) 管**一条结论怎么才算成立**（证据链、可信度、对抗证伪）。
本文管**成立之后怎么写下来**：地址怎么引、路径怎么写、版本怎么声明、表格装什么。

两者分工别混：METHOD 是认识论，DOCSTYLE 是排版与可检索性。

## 为什么需要它

拆库时清点出来的实际状况：

| 症状 | 规模 |
| --- | --- |
| 地址引用的拼法 | 1652 处，**12 种**（`0x431510` / `` `0x431510` `` / `@0x431510` / `@ 0x431510` / `` @`0x431510` `` …） |
| 失效的路径引用 | **315 处**，横跨 57 个文件，在仓库里躺了几个月无人发觉 |
| 表格里塞整段推理的单元格 | 7 处（最长 291 字符） |

这些不是审美问题。地址拼法不统一 → `grep` 不出完整证据；路径失效 → 引用链断掉而无人知道；
表格塞长文 → 既不可 grep 也不可 diff。

> **一条不被脚本检查的规范，半年后就是一份考古材料。**

所以每条规则都配了检查：[`tooling/check-docs.py`](tooling/check-docs.py)。**改规范就要同步改它。**

```bash
python tooling/check-docs.py              # 全部检查
python tooling/check-docs.py --only addr  # links / addr / version / tables
python tooling/check-docs.py --orphans    # 附带列出无入链文档
```

---

## 1. 地址

**写成 `` `0x4919a0` ``。** 一律反引号包裹，不带 `@`，十六进制 6–8 位。

```
✅  `sht_parse_resolve_funcptrs` `0x443790`
❌  sht_parse_resolve_funcptrs @0x443790
❌  `sht_parse_resolve_funcptrs` @ **0x443790**
```

**少于 6 位十六进制的不是地址，是偏移/大小，保持原样**：`+0x165a8`、`0x108` 字节、`stride 0x94`。

**哨兵常量豁免**：`0xFFFFFFFF`、`0x00000000`、`0x400000`(imagebase) 不算地址，不要求版本前缀。

> 检查：`--only addr`（裸地址、`@` 前缀）

## 2. 版本

**引用了真实地址的文档，必须在开头 20 行内声明默认版本。**

```markdown
> **版本**：TH16 v1.00a（`th16.exe`，imagebase `0x400000`）。本文裸地址默认属该版本；引用其他版本须写成 `th18:0x…`。
```

声明之后，正文里的裸地址 `` `0x442560` `` 默认属于该版本——**不必逐个加前缀**（没人愿意打 800 遍 `th16:`）。

**跨版本文档**（OVERVIEW、格式参考、METHOD 等）声明：

```markdown
> **版本**：跨版本。本文出现的地址一律带版本前缀（如 `th16:0x442560`）。
```

此时**每个地址都必须带前缀**——因为这类文档正是最容易把某作结论误当通用的地方，
而那恰好是 [`METHOD.md`](METHOD.md) 要防的头号错误。

副产品：`grep -r 'th18:' engine/` 就能列出 TH18 的全部一手证据。

> 检查：`--only version`（缺声明；跨版本文档里的裸地址）

## 3. 路径

| 场景 | 写法 | 理由 |
| --- | --- | --- |
| **markdown 链接** | 相对路径 `[x](../sht/th16/03-….md)` | 能被检查器验证；点得动 |
| **散文里提到某文件** | **仓库根相对** `` `engine/sht/th16/03-….md` `` | 散文路径没有工具校验，根相对至少在文档移动时不失效 |
| **指向 THTK-Studio 仓库** | 写全 “THTK-Studio 仓库的 `docs/`” | 它是另一个仓库，`../` 到不了 |

拆库时改掉的 315 处，就是"散文里写文档相对路径"的代价。

> 检查：`--only links`（链接目标必须存在）

## 4. 表格

**表格是索引，不是容器。一格 = 一个短句（上限 200 字符）。**

出现「、；。」串联的长推理，说明它该是一个小节：表格里只留名字 + 地址 + 链接，
推理搬到下面开 `### `。

现存 7 处历史欠账登记在 [`tooling/docs-baseline.txt`](tooling/docs-baseline.txt)，
用**棘轮**管理：已登记的不报错，**新增的直接报错**。改好一处就从基线里删一行——
只允许变少。键是「相对路径 + 单元格内容」的哈希，与行号无关，所以无关编辑不会让它失效。

```bash
python tooling/check-docs.py --update-baseline   # 只在有意接受新欠账时用
```

## 5. 可信度标记（这条查不了，靠自觉）

**标记挂在「断言」上，不挂在「小节」上。**

```
✅  | 生命是 5 态状态机 | ✅ | 🟡 | th16/01 §1 |
✅  - 决死窗口 8 帧 ✅（`0x442560` switch 分支实证）
❌  ## 2. 对象模型 ✅/🟡        ← 到底哪句是 🟡？
```

图例统一为：**✅ 一手验过（附地址）· 🟡 待验/单源 · ❌ 已知不同或不存在 · ❓ 存疑 · — 未看**。

只解开了某个分支，别写成「整个字段已解」——完整闸门见 [`METHOD.md`](METHOD.md)。

---

## 新建文档的起手式

```markdown
# <子系统> — <一句话说清是什么>

> **版本**：TH16 v1.00a（`th16.exe`，imagebase `0x400000`）。本文裸地址默认属该版本；引用其他版本须写成 `th18:0x…`。
> **方法**：<怎么得到的:一手反编译 / ExpHP 命名 / 社区交叉>
> **可信度**：<✅/🟡 分布>

## 0. 一句话结论
...
```

写完跑一遍 `python tooling/check-docs.py`。
