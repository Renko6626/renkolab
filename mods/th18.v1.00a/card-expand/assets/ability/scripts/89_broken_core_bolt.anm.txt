script script89 {
    // 破损核心的闪电弹 —— shooter 的 `+0x22` 指这里（装备卡子机开火时 anm 源被换成 ability.anm）。
    // rotateAuto(1)：Player__tick_player_bullets `0x45edb0` 每帧把 VM 旋转设成弹的实时角度（= func_on_init 5
    // 用 player+0x479cc 覆写过的瞄准角），所以贴图画成朝 +x 就够，C 不用碰旋转。
    // interruptLabel(1) = 命中/消失，照零售 script17 / script21。
-1:
    layer(13);
    sprite(@BROKEN_CORE_BOLT);
    type(1);
    alpha(255);
    blendMode(1);
    zWriteDisable(1);
    rotateAuto(1);
    scale(0.30f, 0.60f);
0:
    scaleTime(6, 4, 0.60f, 0.60f);       // 出膛时横向拉开
+6: // 6
flicker:
    alphaTime(3, 0, 208);
+3: // 9
    alphaTime(3, 0, 255);
+3: // 12
    jmp(flicker, 6);
    stop();
    interruptLabel(1);
    layer(15);
    blendMode(1);
    scaleTime(10, 4, 1.10f, 1.10f);
    alphaTime(10, 0, 0);
+10:
    delete();
}
