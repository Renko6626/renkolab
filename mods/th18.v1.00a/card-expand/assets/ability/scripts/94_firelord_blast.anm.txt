script script94 {
    // 火球爆炸：C 在火球到点那帧起它、钉在落点（纯特效，没有判定——伤害是 C 连续 12 帧放的定点伤害源）。
    // 贴图自带内圈闪光 + 外圈光环；20 帧从 0.4 放大到 2.4（128 px 贴图 → 约 300 px，罩住 96×96 的判定有余）并淡出；同帧 C 还起 script96 火星 + 震屏。
-1:
    layer(13);
    sprite(@FIRELORD_BLAST);
    type(1);
    blendMode(1);
    zWriteDisable(1);
    alpha(255);
    scale(0.4f, 0.4f);
0:
    scaleTime(20, 5, 2.4f, 2.4f);
+5: // 5
    alphaTime(15, 0, 0);
+15: // 20
    delete();
}
