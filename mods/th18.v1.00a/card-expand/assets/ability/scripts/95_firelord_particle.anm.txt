script script95 {
    // 火球飞行中的橙白粒子：C 每帧起 FL_PARTICLES 颗、钉在火球当前坐标；每颗在这里用 ANM 自己的随机（UI RNG 流，与 replay 无关）
    // 抽一个飘散方向，27 帧内飘 ±16 px、从白热缩小淡成橙红（2026-09-06 用户：尺寸略大、时长 +50％）。借火球贴图（白芯 + 橙边，缩小后就是一颗火星）。
-1:
    layer(13);
    sprite(@FIRELORD_FIREBALL);
    type(1);
    blendMode(1);
    zWriteDisable(1);
0:
    fsetMul(%F0, %RANDF, 32.0f);
    fadd(%F0, -16.0f);
    fsetMul(%F1, %RANDF, 32.0f);
    fadd(%F1, -16.0f);
    fsetMul(%F2, %RANDF, 0.16f);
    fadd(%F2, 0.22f);
    color(255, 250, 230);
    alpha(230);
    scale(%F2, %F2);
    posTime(27, 0, %F0, %F1, 0.0f);
    scaleTime(27, 0, 0.04f, 0.04f);
    colorTime(27, 0, 255, 120, 40);
    alphaTime(27, 0, 0);
+27: // 27
    delete();
}
