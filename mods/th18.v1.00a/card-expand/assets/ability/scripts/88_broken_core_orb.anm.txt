script script88 {
    // 破损核心（id 71）的电球子机 —— 由 Player__allocate_option 起（脚本号当它的最后一个栈参）。
    // 骨架照零售子机脚本 script2 / script16：interruptLabel(2) 出场、(3) 收起（开店 FUN_00416cd0 发的）、(1) 销毁。
    // 出场时加色混合、落定后切回普通混合（照 script2：用户的球是实心贴图，常驻加色会在亮背景上发白），慢自转 + 亮度呼吸。
-1:
    layer(13);
    blendMode(1);
    type(1);
    zWriteDisable(1);
0:
    interruptLabel(2);
    sprite(@BROKEN_CORE_ORB);
    scale(0.0f, 0.0f);
    scaleTime(10, 4, 0.34f, 0.34f);
    angleVel(0.0f, 0.0f, 0.02617994f);   // 每帧 1.5°：慢慢自转
    alpha(255);
+10: // 10
    scaleTime(6, 1, 0.28f, 0.28f);       // 球占贴图 112/128，场上约 31 px
+6: // 16
    blendMode(0);
breathe:
    alphaTime(20, 4, 176);
+20: // 36
    alphaTime(20, 4, 255);
+20: // 56
    jmp(breathe, 16);
    stop();
    interruptLabel(3);
    scaleTime(20, 4, 0.0f, 0.0f);
+20:
    stop();
    interruptLabel(1);
    blendMode(1);
    scaleTime(20, 4, 0.0f, 3.0f);
+20:
    delete();
}
