# TH18 新增卡牌的硬边界 —— 每一处被零售卡集合写死的地方

> **版本**：TH18 v1.00a（`th18.exe`，imagebase `0x400000`）。本文裸地址默认属该版本；引用其他版本须写成 `th16:0x…`。
> **性质**：**前期调研**，不含改造方案的实施。回答一个问题——
> 「想加一张能进商店池、能在菜单里显示的新卡，会撞上什么？」
> **可信度**：标 ✅ 的是本次一手反汇编读出的；标 🟡 的来自 ExpHP 结构体尺寸，**未逐个一手复核**。

## 0. 结论先行

**零售卡集合的规模以 `56 / 57 / 58` 三个数字散落在至少 12 处**，其中 4 处是代码里的
立即数、8 处是结构体尺寸。**没有任何一处是「读表算出来的」**——全部写死。

更关键的是：**按 card_id 索引的数组几乎没有余量**。`zAbilityManager` 里那个「本局是否
拥有」的数组占到 `+0xd64`，而对象在 `+0xd70` 结束——**只剩 12 字节**。

所以「往 `zTableCardData[]` 后面追加一项」这条路**不成立**，不是因为表在 `.rdata`
不可写（thcrap 写得了），而是因为**表长只是十几处硬编码之一**。

## 1. 先把三个数字分清

| 数字 | 含义 | 出处 |
| --- | --- | --- |
| **58** | `zTableCardData[]` 的条目数（`0x4c53c0`，stride `0x34`） | [`07-registry.md`](07-registry.md) |
| **57** | 可分配的 card_id 数（0–56）；跳转表项数；解锁位数组长度 | 本文 §2 #1 #2 #10 |
| **56** | 真正的「卡」——58 减去 `NULL`/`BACK` 两个菜单哨兵。商店池、菜单数组、全收集成就都按它 | §2 #7 #9 #12 |

**id 56/57 是哨兵，不是卡**（`07-registry.md` 表末两行）。`TableCardData__get` 查不到
时正是返回 56 号那个 `NULL` 条目当安全值。

## 2. ★ 边界全表

| # | 位置 | 边界 | 形式 | 撞上会怎样 | 可信度 |
| --- | --- | --- | --- | --- | --- |
| 1 | `AbilityManager__allocate_new_card` `0x411460` | `cmp ebx,0x38` → id ≤ 56 | 代码立即数 | **id ≥ 57 直接失败返回 -1**，卡创建不出来 | ✅ |
| 2 | 同上的跳转表 `0x412dac` | 57 项（id→类） | `.rdata` 表 | 新 id 无处分派 | ✅ |
| 3 | 同上 `cmp [this+0x28],0x100` | 同时持有 ≤ 256 张 | 代码立即数 | 宽松，**不是瓶颈** | ✅ |
| 4 | `TableCardData__get` `0x407d70` | 循环止于绝对地址 `0x4c5f8c` | 代码立即数 | 新条目查不到；未知 id 返回 56 号哨兵（不崩） | ✅ |
| 5 | `ability.txt` 解析器 `0x4160b0` | 循环止于 `0x4c5f88` | 代码立即数 | 新卡的文案**不会被装载** | ✅ |
| 6 | 文案缓冲 `zAbilityText` | `0x63e0` 字节 ≈ 56 卡 × 7 行 × `0x40` | 结构尺寸 | 越界写 | 🟡 |
| 7 | `CardShop__pick_weighted_random_offer` `0x416f50` | 遍历 `mgr+0xc84` 到 `< 0xd64`（56 项） | 代码立即数 | **新卡不进商店随机池** | ✅ |
| 8 | `zAbilityManager` 本局拥有数组 `+0xc84` | 56 个 dword，对象 `+0xd70` 结束 | 结构尺寸 | **只剩 12 字节余量** | ✅ |
| 9 | `zAbilityMenu.__card_ids_0x304` | `int32_t[0x38]` = 56 | 结构尺寸 | 菜单（图鉴/卡组）列不下 | 🟡 |
| 10 | `zScoreFile.unlocked_cards` `+0x5f588` | `uint8_t[0x39]` = 57 | 结构尺寸 | 解锁位越界（其后 `0x5f5c1`–`0x5f608` 有 71 字节未知区） | 🟡 |
| 11 | 初始卡组 `+0x5f608` | `uint8_t[4][0x10]` = 每套 **16 张** | 结构尺寸 | 初始携带数量上限 | 🟡 |
| 12 | `CardCollection__mark_obtained_and_notify` `0x418de0` | **前 56 项**全置位 → 成就 `0x1d` | 循环边界 | 新卡会让「全收集」永远达不成 | ✅（[`05`](05-shop-and-money.md) §10）|

### 交叉验证

`allocate_new_card` 的 **57 项跳转表**与 `labels.json` 里 **57 条 `card` 标签**、
以及 [`07-registry.md`](07-registry.md) 的 58 项注册表（减去两个哨兵 = 56 张真卡 + id 0 空白卡 = 57 个可分配 id），
三边独立得出、互相吻合 ✅。`games/th18.v1.00a/INDEX.md` 里那条「该做交叉对名」的待办到此闭合。

## 3. 未知 id 的实际行为（不是「default 分配」）

ROADMAP 里问的是「`allocate_new_card` 的完整 switch/default：未知 ID 当前如何处理」。
答案是：**根本走不到 switch**。

```asm
0x411460  cmp dword [edi+0x28], 0x100    ; 已持有 >= 256 张?
          jge  失败
          mov  ebx, [ebp+8]              ; card_id
          cmp  ebx, 0x38                 ; > 56 ?
          ja   失败                      ; ← 无符号比较,新 id 在这里就被挡掉
          jmp  dword [ebx*4 + 0x412dac]  ; 57 项跳转表
...
失败:     or   eax, -1                   ; return -1
成功:     inc  dword [edi+0x28]          ; num_total_cards++
          mov  eax, [edi+0x28]           ; 返回新的持有数
```

所以**二进制里没有一个可单独接管的 default 标签**——编译器把 `default: return -1`
和「持有数 ≥ 256」的失败路径合并成了同一个出口（`0x412da0`），`ja` 就是跳转表的范围检查。
⚠️ 别据此以为源码里也没有 default，源码里有，见
[`11-sentinels-56-57.md`](11-sentinels-56-57.md) §4.6.2。

要接自定义分配，只有两个位置：在 `ja` 之前 hook（改判定 + 自己分派），或者整体重指跳转表。

好消息是它**失败得很干净**：返回 -1、不崩、不留半个对象。

## 4. 好消息：文案是文本文件

`FUN_004160b0` `0x4160b0`（由 `AbilityManager__operator_new` `0x408439` 调一次）解析
**`ability.txt`**：

- 按 `@<internal_name>` 分块，用**内部名字符串**在注册表里线性查该卡；
- 每卡读最多 **7 行**，逐行写进 `(card_id * 7 + 行号) * 0x40 + 缓冲区`。

即**卡牌名称/说明来自游戏归档里的一个文本文件**，不是硬编码字符串。
这正是 thcrap 最擅长的东西（文件替换是它的本职），所以**文案侧几乎没有障碍**——
前提是 #5 的循环边界和 #6 的缓冲区尺寸放得进去。

内部名是查表键，也意味着新卡必须先在注册表里有一项、且 `internal_name` 指针有效。

## 5. 图标

`zTableCardData` 的 `sprite_large` `+0x2c` / `sprite_small` `+0x30` 是 **`abcard.anm` 的 sprite 索引**。
三个卡牌 ANM（`zAbilityManager` `+0x0c`/`+0x10`/`+0x14`）各装什么——thanm release 12 一手解包（2026-09-04）：

| 文件 | entry / sprite | 脚本 | 装什么 |
| --- | --- | --- | --- |
| `abcard.anm` | 118 / 118（**一 entry 一 sprite，号相同**） | 18 | 0/1 卡框与道具图；2..117 卡图 `_max` 256×320 / `_min` 64×80 成对；116/117 = `empty`/`dummy` |
| `ability.anm` | 7 / 267 | 68 | 场上特效（借 player / bullet 素材）；主动卡效果脚本在这（Tenshi 用 0x1c） |
| `abmenu.anm` | 3 / 43 | 19 | 卡组编成 / 图鉴 UI |

**余量**：格式层面 sprite 数只是 header 计数，追加无上限；新卡从 118 起两两追加（card-expand 已加到 127，
`mods/th18.v1.00a/card-expand/assets/`）。**运行时**是否对 sprite 数设上限 🟡 未验——要看 `AnmManager__preload_anm`
如何为 `AnmLoaded` 分配 sprite 表；实跑 118–127 能显示即为下界证据。分发走 thcrap 整文件替换（`th18/abcard.anm`）。

## 6. 三条可能的路线（判据，不是方案）

| 路线 | 做法 | 代价 |
| --- | --- | --- |
| **A. 换皮** | 不加新 id，改写某张零售卡的效果/文案/图标 | 边界一个都不撞。ROADMAP 阶段 1 就是它 |
| **B. 吃掉哨兵** | 拿 id 56/57 两个菜单哨兵当新卡用 | ❌ **已判死**，见 [`11-sentinels-56-57.md`](11-sentinels-56-57.md) |
| **C. 整体搬迁** | 把注册表 + 所有按 id 索引的数组搬进 codecave，binhack 掉 §2 全部立即数 | 12 处边界、其中 8 处是**结构体内部的数组**——意味着 `zAbilityManager` / `zAbilityMenu` / `zScoreFile` 都要扩容或重指，牵连**存档格式** |

**C 的真正难点不是表，是存档。** `unlocked_cards` 和 `initial_cards_per_shottype` 都在
`zScoreFile` 里，而 `zScoreFileSuper` 还有一份 `backup` 副本（`+0x5f888`）。动它就动存档
兼容性——老存档、以及别人的存档。

**B 的可行性已调查完毕（2026-09-01）：不可行。**id 56 是全部内联查表的「查不到」回退行、
兼卡组编成里的「空槽」伪卡；id 57 是图鉴里未解锁卡的卡背图，而且 `allocate_new_card`
根本不放行它。全部证据见 [`11-sentinels-56-57.md`](11-sentinels-56-57.md)。

于是只剩 A 和 C。C 的机械工价现在算得出来了（全二进制线性反汇编穷举，同篇 §4.5）：
**只搬表 99 处 / 9 个函数，全部改完 132 处 / 15 个函数**。

更根本的解释也在那一篇：**56 不只是「数量」，还是一个写进存档的「值」**——
初始卡组数组被 `memset(…, 56, 48)` 填成空槽，所以每一份现有存档里都写着 56。

## 7. 本次未查（下一轮的题目）

- 三个卡牌 ANM 的 sprite 索引空间余量。
- `zScoreFile` 中 `0x5f5c1`–`0x5f608` 那 71 字节未知区是否真的空闲。
- replay 里卡组的存储格式与长度上限（[`05`](05-shop-and-money.md) §10 只确认了存/复原的调用点）。
- #6 #9 #11 三条 🟡 需要一手复核（目前只有 ExpHP 的结构体尺寸）。
  #10 已在 [`11-sentinels-56-57.md`](11-sentinels-56-57.md) §5 转正；
  #7 #8 #12 同篇一并转正。
